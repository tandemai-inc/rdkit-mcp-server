# Specify tool names or modules to allow (specifying module allow all tools within)
allow_list:
  - ExactMolWt
  - rdkit_mcp.Chem.Descriptors

# Specify tool names or modules to block (specifying module block all tools within)
block_list:
  - ExactMolWt
  - rdkit_mcp.Chem.Descriptors

# Directory where tools can write output files
FILE_DIR: /home/user/workspace/rdkit-mcp-server/outputs/
